# Copy to `.env` for local dev. On Railway, set these in Project → Variables.

# Required — Kinguin store API (product search, catalog)
KINGUIN_API_KEY=

# Optional — Telegram bot (admin notifications / commands)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Comma-separated numeric user IDs
TELEGRAM_ADMIN_IDS=
