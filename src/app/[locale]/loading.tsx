import RouteLoadingLogo from "@/components/layout/RouteLoadingLogo";

/** Route transition: logo pulse — page shell stays visible. */
export default function Loading() {
  return <RouteLoadingLogo />;
}
